name: ETF Monitor

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run ETF monitor
        env:
          ETF_CODES: ${{ secrets.ETF_CODES }}
          ETF_NAMES: ${{ secrets.ETF_NAMES }}
          PROXY_URL: ${{ secrets.PROXY_URL }}
          PUSH_MODE: ${{ secrets.PUSH_MODE }}
          BARK_URL: ${{ secrets.BARK_URL }}
          BARK_GROUP: ${{ secrets.BARK_GROUP }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_GROUP: ${{ secrets.TELEGRAM_GROUP }}
        run: python etf_dingtou_monitor.py
